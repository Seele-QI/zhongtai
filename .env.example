# 复制本文件为 .env 并填入真实 Key（.env 已被 git 忽略）
# 「视频制作 → 声音克隆」页顶栏开源出处链接（仅该页展示；公开变量，会打进前端 bundle）
# NEXT_PUBLIC_OPEN_SOURCE_REPO_URL=https://github.com/your-org/your-repo
TIANAPI_KEY=你的天聚数行 Key
# 小红书爆款制造机「流式对话」、爆改 /rewrite 等（见 app/api/ai/chat-stream/route.ts）
# 生图大师 / 品牌工作台「智能调整」：无参考图时用 DeepSeek；上传参考图后走方舟豆包识图（须配置下方 ARK_ENDPOINT_ID 为多模态在线推理接入点）
# 线上（Netlify 等）：在托管后台「Environment variables」配置同名变量并重新部署；勿使用 NEXT_PUBLIC_ 前缀。
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
RUNNINGHUB_API_KEY=your-runninghub-api-key-here

# —— 火山方舟：品牌工作台 / 生图大师 调用图片生成（app/api/ai/ark-images/route.ts）——
# ARK_API_KEY=控制台「API Key 管理」中的密钥（Bearer）
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# 生图必填：须为当前账号「在线推理」里「图像生成」接入点的服务 ID（OpenAI 兼容 /images/generations）；勿填他人截图示例 ep
# ARK_IMAGE_ENDPOINT_ID=ep-xxxx
# 若该生图服务在「API 接入」里单独下发了 API Key，可填（否则留空，沿用 ARK_API_KEY）
# ARK_IMAGE_API_KEY=
# 对话 / 多模态识图（智能调整含参考图时走 chat/completions，须为支持视觉的豆包等接入点；与下方「图像生成」ARK_IMAGE_ENDPOINT_ID 不是同一个 ep）
# ARK_ENDPOINT_ID=ep-xxxx
# 可选：爆款视频 AI 拆解页（/api/ai/video-teardown-analyze）单独指定豆包 Pro 文本接入点；不填则沿用 ARK_ENDPOINT_ID
# ARK_VIDEO_TEARDOWN_ENDPOINT_ID=ep-xxxx
# 上传图片且配置方舟时，服务端走 Responses API（{BASE}/responses）。
# ARK_API_KEY=「API Key 管理」里创建的密钥（Bearer），不要填接入点 ID。
# ARK_ENDPOINT_ID=「在线推理」里该条接入点的 ID（多为 ep- 开头，与控制台完全一致）。
# ARK_BASE_URL 需与接入点地域一致，例如北京：https://ark.cn-beijing.volces.com/api/v3
# ARK_API_SECRET=可选，与 ARK_API_KEY 二选一作为 Bearer。
# 爆改 / 智能体由 Next 直连 DeepSeek，一般无需 FastAPI。若仍运行 main.py（可选），可保留：
# FASTAPI_URL=http://127.0.0.1:8000

# —— 邮箱登录（Magic Link）——
# Resend API key：https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here
# 发件人地址。开发期可保留默认；上线需要在 Resend 后台验证自有域名
RESEND_FROM=AgentHub <onboarding@resend.dev>
# 公网访问地址（用于邮件中拼接登录链接）。Netlify 部署时填 Netlify URL
APP_PUBLIC_BASE=http://localhost:3000

# —— 邮箱 hash 盐值（首次部署随机生成 32 字节后填入；不要提交到 git）——
# 生成命令：python -c "import secrets; print(secrets.token_hex(32))"
EMAIL_HASH_SALT=replace-with-output-of-secrets.token-hex-32

# —— 积分系统 ——
CREDIT_REGISTER_BONUS=100
CREDIT_SESSION_TTL_DAYS=30
CREDIT_EMAIL_TOKEN_TTL_SECONDS=900

# —— 积分管理后台访问密钥（前端 /admin/credit 登录用；勿提交到 git）——
# 部署时改为强随机字符串，例如：python -c "import secrets; print(secrets.token_urlsafe(24))"
CREDIT_ADMIN_ACCESS_KEY=replace-with-strong-random-secret

# —— 邮件 dev 模式：链接打到服务端日志，不真发。生产必须设 0 ——
DEV_EMAIL_MODE=1

# —— 视频剪辑 BGM 目录（main.py:_resolve_bgm_dir 读取）——
# 存放可被模板混音的 mp3 / wav / aac / m4a 文件；render_video_with_template
# 会按视频裁剪时长选最接近的 BGM（详见 lib/video_postprocess.py）。
# 生产机建议改为绝对路径，例如 /opt/agenthub/bgm
VIDEO_BGM_DIR=assets/bgm
